/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    distDir: 'out', // This is where the static files (including index.html) will be generated
  };
  
  module.exports = nextConfig;
  